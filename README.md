## IMS Platform

### Setup Guide

#### Frontend

First go to the **Frontend** and run
`npm install`

Then run the code using
`ng serve`

#### Backend

First go to the **Backend** and run
`npm install`

Then run the code using
`npm start`
