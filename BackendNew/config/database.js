const dotenv = require("dotenv");

// Configure dotenv for read environment variables.
dotenv.config();

module.exports = {
  HOST: "localhost",
  USER: "root",
  PASSWORD: "password",
  DB: "IMS"
};
